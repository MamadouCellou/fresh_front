import 'package:flutter/material.dart';

class LegumesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Conservation des Légumes',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
