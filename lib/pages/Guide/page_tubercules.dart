import 'package:flutter/material.dart';

class TuberculesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Conservation des Tubercules',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
